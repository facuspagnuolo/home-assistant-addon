name: Annika Connector
version: "1.0.0"
slug: annika_connector
description: Outbound rathole tunnel for Home Assistant
arch:
  - amd64
  - aarch64
startup: application
boot: auto
init: false
options:
  unit_id: "unit-123"
  log_level: info
schema:
  unit_id: "match(^[a-zA-Z0-9_-]+$)"
  log_level: "list(error|warn|info|debug|trace)"
